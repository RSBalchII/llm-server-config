{
  "name": "llm-server-config",
  "version": "1.0.0",
  "description": "Local Llama server for running GGUF models with Qwen Coder integration",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "select": "node select-model.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["llama", "gguf", "qwen", "gemma", "local-llm"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1"
  }
}
